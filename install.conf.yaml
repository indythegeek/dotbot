- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
    ~/.zshrc: zshrc
    ~/.oh-my-zsh: oh-my-zsh
    ~/.p10k.zsh: p10k.zsh

- shell:
    - [git submodule update --init --recursive, Install/Updating submodules]
